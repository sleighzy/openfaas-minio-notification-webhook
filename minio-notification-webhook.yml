version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  minio-notification-webhook:
    lang: golang-http
    handler: ./minio-notification-webhook
    image: minio-notification-webhook:latest

